import { FormEvent, useState } from "react";
import { supabase } from "./lib/supabase";
export default function NewsletterSignup({compact=false}:{compact?:boolean}){const[email,setEmail]=useState("");const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();const value=email.trim().toLowerCase();if(!value||!value.includes("@")){setStatus("Enter a valid email.");return;}setBusy(true);setStatus("");const{data:pub}=await supabase.from("bitbuzz_publications").select("id").eq("slug","srgs").eq("status","active").maybeSingle();if(!pub){setStatus("Newsletter is temporarily unavailable.");setBusy(false);return;}const{error}=await supabase.rpc("subscribe_newsletter",{p_publication_id:pub.id,p_email:value});setStatus(error?"Could not subscribe right now.":"You're on the BitBuzz list. Check your inbox for future editions.");if(!error)setEmail("");setBusy(false)};return <section className={`${compact?"rounded-2xl border border-white/10 bg-white/[.025] p-5":"border-y border-white/10 py-12"} text-white`}><div className={compact?"":"mx-auto max-w-[1120px] px-5 lg:px-8"}><p className="text-[9px] font-bold uppercase tracking-[.2em] text-[#ffe600]">The BitBuzz Brief</p><h2 className="mt-2 font-serif text-3xl tracking-[-.04em]">The good stuff. In your inbox.</h2><p className="mt-2 max-w-xl text-xs leading-5 text-white/35">Occasional highlights from space, technology, cybersecurity, aviation and innovation. No spam.</p><form onSubmit={submit} className="mt-5 flex max-w-xl gap-2"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="min-w-0 flex-1 rounded-full border border-white/10 bg-white/[.04] px-4 py-3 text-xs outline-none focus:border-white/30"/><button disabled={busy} className="rounded-full bg-white px-5 py-3 text-[10px] font-bold text-black disabled:opacity-50">{busy?"Joining…":"Subscribe"}</button></form>{status&&<p className="mt-3 text-[10px] text-white/45">{status}</p>}<p className="mt-3 text-[9px] text-white/20">By subscribing, you agree to receive BitBuzz newsletter emails. Unsubscribe anytime.</p></div></section>}
